/** Input limits that keep every intermediate value in the safe-integer range (plan §3). */
export const MAX_QTY = 10_000;
export const MAX_UNIT_PRICE_CENTS = 10_000_000; // $100,000
export const MAX_LINES = 50;
